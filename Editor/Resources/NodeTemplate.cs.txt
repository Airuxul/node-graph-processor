using System;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using GraphProcessor;
using System.Linq;

[System.Serializable, NodeMenuItem("Custom/#NAME#")]
public class #SCRIPTNAME# : BaseNode
{
	// Set to your RuntimeBaseNode type, e.g. typeof(Runtime#SCRIPTNAME#)
	public override Type RuntimeNodeType => null;

	[Input(name = "In")]
    public float                input;

	[Output(name = "Out")]
	public float				output;

	public override string		name => "#NAME#";

	// SO node only describes ports. Create Runtime#SCRIPTNAME# : RuntimeBaseNode for execution logic.
}
